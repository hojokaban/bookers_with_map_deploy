<!-- recieves collection(comments) -->

<td><%= comment.content %></td>

<% if comment.user_id == current_user.id %>
<td><%= link_to "Delete", comments_path(comment), method: :delete, "data-confirm" => "本当に消しますか？", class:"btn btn-danger" %></td>
<% end %>